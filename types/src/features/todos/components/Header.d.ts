/// <reference types="react" />
import PropTypes from 'prop-types';
export declare const Header: {
    ({ hasTodos }: {
        hasTodos: boolean;
    }): JSX.Element;
    propTypes: {
        hasTodos: PropTypes.Validator<boolean>;
    };
};
